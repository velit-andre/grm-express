
const config = require('./config')
const cors = require('./cors')
const database = require('./database')
const routes = require('./routes')
const server = require('./server')
      
module.exports = { config, cors, database, routes, server }