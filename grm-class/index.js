const auth = require('./grm.auth')
const crud = require('./grm.crud')
const functions = require('./grm.functions')
const login = require('./grm.login')
const main = require('./grm.main')
const restful = require('./grm.restful')
const scheduler = require('./grm.scheduler')
      
module.exports = { auth, crud, functions, login, main, restful, scheduler}